console.log("5" - 2);
// in case of plus if the string is exit in between a + operator 
// number is converted into string the concatenate but in case of the - string is converted into number 
// then perform operation :

// 5 is converted into  number then perform  simple maths operation 


console.log(typeof null); // object
// null is the object :

// internally null is refers to 000 :
// in the js 000 tag is for the object reference bec it has 3 000 so this return object 