console.log([] == 0);   // true
console.log([] === 0); // false


// [] is the falsy value 
// whenever we compare with 0 then [] is converted into the 0 then it check 
// == internally to type coercion type casting 

