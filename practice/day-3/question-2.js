const arr = [1, 2, 3];  
console.log(arr.length); // length is the properties 

// mutate the  length of the array 
arr.length = 1;
// here we try to modify  or mutate the length of the array  

console.log(arr) // [1]; 

// array is the object 
// array is mutable in nature 
// array store in the heap memory 
// array is reference data structure 

