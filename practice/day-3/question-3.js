console.log([] == []);  //false
console.log([] == ![]);// true

// [] this is the array => reference data  , mutable , object 
// []

// [] == []  both array have the different reference due to this region this will be false 
// bec both array have the diff. memory address 
